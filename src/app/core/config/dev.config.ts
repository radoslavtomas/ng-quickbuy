import type { BrandId } from '../models/brand.model';

/**
 * Development configuration.
 *
 * Change DEFAULT_BRAND_ID to test a different brand when running on localhost.
 * Valid values: 'qld' | 'chq' | 'ajg'
 */
export const DEFAULT_BRAND_ID: BrandId = 'ajg';
